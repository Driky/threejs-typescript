const config = {
    rendererW: window.innerWidth,
    rendererH: window.innerHeight,
    cameraViewAngle: 45,
    cameraNear: 0.1,
    cameraFar: 10000,
    domSelector: "#container"
};

export default config;
